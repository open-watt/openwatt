# STM32 system configuration -- baked into binary via string import
# Runs before user startup.conf
#
# Default console on USART1 (PA9 TX, PA10 RX).
# Nucleo boards: change to uart1 for USART2 (ST-Link VCP on PA2/PA3).

/stream/serial add name=console device=uart0 baud-rate=115200
