# SmartEVSE board-safe defaults. The board driver begins disabled, keeping the
# pilot at a steady +12 V and both contactors open until explicitly enabled.
/system/update-rate rate=100
/driver/boards/smartevse/add name=evse current=60 enabled=false
