# BK7231N system configuration -- baked into binary via string import
# Runs before user startup.conf
# BK7231N: Wi-Fi 802.11b/g/n + BLE 5.0

/stream/serial add name=console device=uart0 baud-rate=115200
