# BK7231T system configuration -- baked into binary via string import
# Runs before user startup.conf
# BK7231T: Wi-Fi 802.11b/g/n only (no BLE)

/stream/serial add name=console device=uart0 baud-rate=115200
